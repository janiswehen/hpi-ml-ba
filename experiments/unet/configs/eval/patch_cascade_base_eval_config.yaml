model_type: "patch_cascade_unet"
data_loading:
  n_workers: 0
  patch_size: 16
  split_ratio:
    train: 0.6
    val: 0.2
    test: 0.2
  seed: 42
model_loading:
  path1: "final/cascade1-unet/brain_tumor_unet_weights.pth"
  path2: "final/cascade2-unet/brain_tumor_unet_weights.pth"
eval:
  n_steps: 200 # epoch * (n_samples / batch_size)
  slice_axis: 2
logging:
  enabled: True
  prediction_log_count: 10
