model_type: "full_unet"
data_loading:
  n_workers: 4
  batch_size: 1
  split_ratio: 0.8
  seed: 42
model_loading:
  enabled: False
  path: ""
training:
  n_corrections: 90000 # epoch * (n_samples / batch_size)
  learning_rate: 0.0003
logging:
  enabled: True
  prediction_log_count: 10
